# Академия СКС — проект-контекст для Claude Code

> Этот файл читается автоматически в начале каждой сессии в этой папке.
> Здесь — всё, что нужно знать, чтобы продолжить работу без пересказа истории.

## Что это за проект
Обучающий портал группы компаний **СКС** (ломбарды) по работе с ИИ.
Сейчас это **одностраничный сайт** про Claude Code для офисных сотрудников.
**Цель развития:** превратить в многостраничную академию по направлениям
группы компаний (коммерческий блок, управление персоналом, Claude Code и т.д.).

- **Автор/владелец:** Тимур Хазиев, отдел аналитики СКС.
- **Аудитория:** коллеги разных профессий и уровней подготовки.

## Где живёт и как публикуется
- **Репозиторий:** https://github.com/tikhaziev-coder/sks-claude-academy
- **Живой сайт (GitHub Pages):** https://tikhaziev-coder.github.io/sks-claude-academy/
- **Папка на Mac:** `~/sks-claude-academy`
- **Публикация:** GitHub Pages раздаёт из ветки **`main`**.
  Чтобы обновить сайт — коммит и `git push origin main`. НЕ в фиче-ветку
  (иначе сайт не обновится). После пуша Pages пересобирается ~1–2 мин.
- **Проверка сборки:** `gh api repos/tikhaziev-coder/sks-claude-academy/pages/builds/latest`
  (ждём `"status": "built"` и нужный commit sha).
- **gh / git:** `gh` установлен в `~/.local/bin` (PATH прописан в `~/.zshrc`),
  залогинен как `tikhaziev-coder`, git-auth через `gh auth setup-git`.

## Структура файлов
- `index.html` — весь сайт (~1420 строк, инлайн CSS+JS, один файл).
- `logo.svg` — логотип СКС (красный #E31E24), используется как заставка видео.
- `metodichka.pdf` — методичка, доступна для скачивания/печати с сайта.
- `kb/` — материалы базы знаний (напр. «Связка GitHub и Obsidian.md»).
- `РАЗВЕРТЫВАНИЕ.md` — заметки по деплою.
- `supabase-auth.sql` — схема авторизации (таблица `profiles`, триггер, RLS).
  Уже выполнен в базе 21.07.2026 — повторно запускать не нужно (безопасен для повтора).

## Как устроен index.html
Дизайн-токены (CSS-переменные в `:root`, тёмная тема по умолчанию + светлая):
```
--bg:#130d0e  --bg2:#1b1314  --card:#231819  --card2:#2b1e1f
--ink:#f6ece9  --muted:#c3a9a4
--brand:#e31e24 (красный СКС)  --brand2:#e0a32a (золотой)  --accent:#e8833a
--grad:linear-gradient(135deg,var(--brand),var(--brand2))
--r:18px (радиус)  --maxw:1180px (ширина контента)
```
- `<html data-theme="light|dark" data-level="1">` — тема и уровень рулят CSS.
- Контент-обёртка: `.wrap` (max-width: --maxw, центр). Секции — `<section id="...">`.
- Шапка `<nav class="nav-in">`: логотип + меню + переключатель темы,
  всё **по центру**, в 3 ряда (`flex-basis:100%` у каждого блока).
- Заголовок секции — `.section-head`; карточки — `.card`.
- Адаптив: брейкпоинты 640 / 760 / 880 / 980 px.

### Секции по порядку (id)
start · what · onramp · cap · slides · obsidian · github · kb · library ·
**video** · prompts · prof · how · **materials** · run · safety · quiz · faq

### Видеоплеер (секция #video)
Один плеер + плейлист. Два источника:
- **YouTube** → iframe `youtube-nocookie.com/embed/<id>?autoplay=1&rel=0`
  (click-to-play, иначе ошибка 153 на file://; на HTTPS-Pages всё ок).
- **Google Drive** → iframe `drive.google.com/file/d/<id>/preview`.
  У Drive-видео нет родного превью → показываем заставку `logo.svg`
  через `showPoster(id,type)`, по клику → `play(id,type)`.
- Текущий плейлист (5): «Встреча по ИИ №1» (drive `1mt-T7HCdGriKUlojquymm9rYaJRjXL6y`,
  первое/активное) · YouTube VZNzZAO7v8w · 6NK4Pona2fY · htFmIVif_qg · EHrqzvKmszo.

### Материалы (секция #materials)
`.mat-grid` — сетка `repeat(3,1fr)`, строго по 3 в ряд (2 @880px, 1 @640px).
Ссылки на внешние статьи (Habr, Pikabu, поисковая лента Habr).

### Авторизация (Supabase Auth, добавлена 21.07.2026)
- Вход на сайт закрыт оверлеем `#authGate` (вкладки Вход/Регистрация); контент
  виден только после логина. Это «мягкая заглушка»: контент есть в исходнике.
- Регистрация: ФИО (проверка «минимум 2 слова») + email + пароль,
  метаданные `full_name` → `supa.auth.signUp`. Подтверждение по почте ВКЛЮЧЕНО
  (лимит бесплатного тарифа ~3–4 письма/час). Ошибки переведены на русский (`ruError`).
- Supabase-проект: `iwlmgebsfmkfqcwtuyac`. В Auth → URL Configuration прописаны
  Site URL (Pages) и redirect `http://localhost:8765/` для локальных тестов.
- Таблица `public.profiles` (ФИО, email, дата) заполняется триггером
  `on_auth_user_created`; RLS: свой профиль — каждый, весь список — только
  админ из `public.is_admin()`.
- **Админ:** `ti.khaziev@gmail.com` — задан в ДВУХ местах: SQL `is_admin()`
  и JS-константа `ADMIN_EMAILS` в index.html. Менять синхронно.
- Админ видит в меню «📊 Статистика» (`#navStats`) — модалка со списком
  регистраций (переиспользует `#modal`). Кнопка выхода — `#logoutBtn` в шапке.
- «Забыли пароль?»: `resetPasswordForEmail` → письмо → возврат на сайт с
  `#type=recovery` → форма нового пароля (`#authFormNew`). Пока флаг `recovery`
  активен, заглушка не снимается, хотя recovery-сессия уже есть.
- ВАЖНО: у залогиненных роль в Supabase — `authenticated`, а не `anon`,
  поэтому для bucket `Kb` продублированы политики read/upload на обе роли —
  без них библиотека статей ломается после входа.
- Если CDN supabase-js не загрузился, заглушка снимается (fail-open) —
  сайт не запирается наглухо.
- **ОТКРЫТЫЙ ВОПРОС (SMTP):** письма идут через встроенную почту Supabase —
  лимит ~3–4 письма/час, для массового запуска мало. План: custom SMTP через
  Gmail `aogksks@gmail.com` (нужен «пароль приложения» Google, у владельца
  21.07.2026 были проблемы с доступом к этой почте — отложено). Настройка:
  Authentication → Emails → SMTP Settings (host smtp.gmail.com, порт 465).
- **ОПУБЛИКОВАНО 23.07.2026 (commit `979b1e8`):** авторизация + все 4 видео +
  портал-хаб `/portal/` сведены и запушены в `origin/main`. Живой сайт теперь
  ТРЕБУЕТ логин. Домен-лок `@sks-lombard.ru` ещё НЕ сделан (вход с любой почтой).
  `supabase-auth.sql` тоже в репозитории (схема БД, без секретов).
- **Портал-хаб `portal/index.html`** — приветственная страница группы компаний
  (герой, «О компании», «Корпоративный дух» с фото-заглушками, карта подразделений,
  контакты). Ссылается на `../index.html` (академию). Цифры о компании — ЗАГЛУШКИ
  («—»/«уточняется»), заполнить выверенными данными перед запуском на всех.

## Важные решения и нюансы (НЕ ломать)
- **FAQ:** секция имеет `id="faq"` (для якоря меню), а внутренний список —
  `id="faqList"` (JS: `getElementById('faqList')`). Раньше был дубль id `faq`,
  из-за чего элементы рендерились за пределами `.wrap` — не возвращать дубль.
- **Скрыто через `style="display:none"`** (по просьбе владельца):
  `.cta` (кнопки в герое), `.levelbar` («Мой уровень»), подсказка в библиотеке,
  `#libCount` (счётчик статей), `#libDrop` (дроп-зона загрузки), `#prog`
  (виджет прогресса в углу — обновлялся при каждом заходе, мешал).
- **ОТКРЫТЫЙ ВОПРОС:** селектор уровня скрыт → курс висит на уровне «Новичок»,
  блоки `data-min="2"`/`data-min="3"` (Уверенный/Продвинутый) не видны.
  Если надо показать весь контент — выставить `data-level` повыше по умолчанию.
- Локальный предпросмотр: `python3 -m http.server 8765` в фоне, открыть
  `http://localhost:8765` (НЕ через `file://` — ломает YouTube-embed).
- Видео нельзя залить через MCP Google Drive (только инлайн-контент) — большие
  файлы владелец заливает на Drive вручную и даёт share-ссылку.

## Стиль работы (важно)
- Общение на русском.
- Перед публикацией показывать, что меняем; пушить только по явной просьбе.
- Правки точечные, в духе существующего кода (инлайн-стили там уже норма).
